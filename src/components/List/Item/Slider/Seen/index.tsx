import * as SC from './style';

const Seen = ({}) => {

    return (
        <SC.Seen>
            <SC.Text>
                Просмотрено!
            </SC.Text>
        </SC.Seen>
    );

};

export default Seen;